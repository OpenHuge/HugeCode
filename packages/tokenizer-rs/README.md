# tokenizer-rs

`packages/tokenizer-rs` is a native crate container, not a workspace package.

The tracked Rust crate for this area lives under [native](./native/). Keep the
top-level directory documented so workspace hygiene checks can distinguish this
container from stale package residue.
