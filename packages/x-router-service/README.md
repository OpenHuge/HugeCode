# x-router-service

`packages/x-router-service` is an incubating module shell, not an active
workspace package.

The directory is retained for environment scaffolding and future promotion work.
Do not treat it as a repo package entrypoint until it has a tracked manifest and
documented ownership.
