name: CodeQL

on:
  push:
    branches:
      - main
      - fastcode
  pull_request:
    branches:
      - main
    paths:
      - "apps/**"
      - "packages/**"
      - "scripts/**"
      - ".github/actions/**"
      - ".github/workflows/codeql.yml"
  merge_group:
    branches:
      - main
    types:
      - checks_requested
  schedule:
    - cron: "20 3 * * 1"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  changes:
    name: Detect CodeQL-relevant changes
    runs-on: ubuntu-latest
    outputs:
      fast_pr_iteration_mode: ${{ github.event_name == 'pull_request' && vars.MERGE_QUEUE_ENABLED != 'false' }}
      javascript_required: ${{ github.event_name == 'schedule' || github.event_name == 'merge_group' || steps.filter.outputs.javascript == 'true' }}
      rust_required: ${{ github.event_name == 'schedule' || github.event_name == 'merge_group' || steps.filter.outputs.rust == 'true' }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 2

      - name: Ensure diff base is available
        if: github.event_name == 'pull_request'
        run: |
          git fetch --no-tags --prune --depth=1 origin \
            +refs/heads/${{ github.base_ref }}:refs/remotes/origin/${{ github.base_ref }}

      - id: filter
        uses: dorny/paths-filter@v4
        with:
          filters: |
            javascript:
              - "apps/code/**"
              - "apps/code-web/**"
              - "packages/code-runtime-client/**"
              - "packages/code-runtime-host-contract/**"
              - "packages/code-runtime-webmcp-client/**"
              - "packages/code-workspace-client/**"
              - "packages/design-system/**"
              - "packages/native-bindings/**"
              - "packages/native-runtime-host-contract/**"
              - "packages/shared/**"
              - "packages/ui/**"
              - "tests/**"
              - "package.json"
              - "pnpm-lock.yaml"
              - "pnpm-workspace.yaml"
              - "turbo.json"
            rust:
              - "packages/*-rs/**"
              - "internal/runtime-policy-rs/**"
              - "**/Cargo.toml"
              - "**/Cargo.lock"

  analyze_javascript:
    name: Analyze (javascript-typescript)
    needs: [changes]
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Skip untouched JavaScript/TypeScript lane
        if: needs.changes.outputs.fast_pr_iteration_mode == 'true'
        run: echo "Skipping JavaScript/TypeScript CodeQL lane because PR fast mode is enabled."

      - name: Skip merge queue synthetic ref upload lane
        if: github.event_name == 'merge_group'
        run: echo "Skipping JavaScript/TypeScript CodeQL lane because merge queue synthetic refs are already covered by PR and main scans."

      - name: Skip untouched JavaScript/TypeScript lane
        if: github.event_name != 'merge_group' && needs.changes.outputs.fast_pr_iteration_mode != 'true' && needs.changes.outputs.javascript_required != 'true'
        run: echo "Skipping JavaScript/TypeScript CodeQL lane because no relevant files changed."

      - name: Initialize CodeQL
        if: github.event_name != 'merge_group' && needs.changes.outputs.fast_pr_iteration_mode != 'true' && needs.changes.outputs.javascript_required == 'true'
        uses: github/codeql-action/init@v3
        with:
          languages: javascript-typescript
          build-mode: none
          queries: security-extended,security-and-quality

      - name: Analyze
        if: github.event_name != 'merge_group' && needs.changes.outputs.fast_pr_iteration_mode != 'true' && needs.changes.outputs.javascript_required == 'true'
        uses: github/codeql-action/analyze@v3

  analyze_rust:
    name: Analyze (rust)
    needs: [changes]
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Skip untouched Rust lane
        if: needs.changes.outputs.fast_pr_iteration_mode == 'true'
        run: echo "Skipping Rust CodeQL lane because PR fast mode is enabled."

      - name: Skip merge queue synthetic ref upload lane
        if: github.event_name == 'merge_group'
        run: echo "Skipping Rust CodeQL lane because merge queue synthetic refs are already covered by PR and main scans."

      - name: Skip untouched Rust lane
        if: github.event_name != 'merge_group' && needs.changes.outputs.fast_pr_iteration_mode != 'true' && needs.changes.outputs.rust_required != 'true'
        run: echo "Skipping Rust CodeQL lane because no relevant files changed."

      - name: Initialize CodeQL
        if: github.event_name != 'merge_group' && needs.changes.outputs.fast_pr_iteration_mode != 'true' && needs.changes.outputs.rust_required == 'true'
        uses: github/codeql-action/init@v3
        with:
          languages: rust
          build-mode: none
          queries: security-extended,security-and-quality

      - name: Analyze
        if: github.event_name != 'merge_group' && needs.changes.outputs.fast_pr_iteration_mode != 'true' && needs.changes.outputs.rust_required == 'true'
        uses: github/codeql-action/analyze@v3
